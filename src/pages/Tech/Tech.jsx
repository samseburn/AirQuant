import { TechExplanation } from 'components';
import React from 'react';

const Tech = () => {
  return (
    <div
      style={{
        display: 'flex',
        width: '100%',
        height: '100vh',
        justifyContent: 'center',
        alignItems: 'center',
      }}
    ></div>
  );
};

export default Tech;
